// Sum all numbers from an array

'use strict';

allNumbersSum = (numbers) => {
  resultOfNumbersSum = 0;
  for (i of s) {
    let t = typeof i;
    if (t === 'number') {
      }
    }
};

module.exports = allNumbersSum;
